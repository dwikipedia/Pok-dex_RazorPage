@page
@model Pokédex.Pages.PokemonList.IndexModel
@{
    ViewData["Title"] = "Pokemon List";
}


<div class="container p-0 mt-2">
    <div class="bg-light row">
        <div class="col-md-10">
            <h2 class="text-info">@ViewData["Title"]</h2>
        </div>
        <div class="col-md-2 mt-1">
            <a asp-page="Create" class="btn btn-md btn-info text-white">
                Add new
            </a>
        </div>
    </div>

    <div class="col-12 border p-3 m-3">
        @*<form method="post">
            @if (Model.Pokemons.Count() > 0)
            {
                <table class="table table-bordered" id="datatable">
                    <tr class="table-primary text-center">
                        <th><label asp-for="Pokemons.FirstOrDefault().Name"></label></th>
                        <th><label asp-for="Pokemons.FirstOrDefault().Gender"></label></th>
                        <th><label asp-for="Pokemons.FirstOrDefault().Type"></label></th>
                        <th>Actions</th>
                    </tr>
                    @foreach(var i in Model.Pokemons)
                    {
                        <tr>
                            <td>@Html.DisplayFor(m=> i.Name)</td>
                            <td>@Html.DisplayFor(m=> i.Gender)</td>
                            <td>@Html.DisplayFor(m=> i.Type)</td>
                            <td class="text-center">
                                <button asp-page-handler="Delete" asp-route-id="@i.PokemonId"
                                        onclick="return confirm('Are you sure?')" class="btn btn-sm btn-danger mr-3"> Delete</button>
                                <a asp-page="Edit" asp-route-id="@i.PokemonId" class="btn btn-warning btn-sm"> Edit</a>
                            </td>
                        </tr>
                    }
                </table>
            }
            else
            {
                <p>No data available. </p>
            }
        </form>*@

        <table id="Datatable" class="table table-striped table-bordered" style="width:100%">
            <thead class="text-center">
                <tr>
                    <th>Name</th>
                    <th>Gender</th>
                    <th>Type</th>
                    <th>Actions</th>
                </tr>
            </thead>
        </table>
    </div>
</div>


@section Scripts{ 
    <script src="~/js/Pokemon/pokemonList.js"></script>
}